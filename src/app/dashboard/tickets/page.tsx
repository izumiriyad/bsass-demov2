import type { Metadata } from "next";
import { getSessionUser } from "@/lib/auth";
export const metadata: Metadata = { title: "Support Tickets" };
const tickets=[['TCK-1001','bKash deposit pending','Open'],['TCK-1002','KYC document review','Waiting'],['TCK-1003','Promotion eligibility','Resolved']];
export default async function TicketsPage(){const user=await getSessionUser(); if(!user) return null; return <div className="space-y-5"><div><h1 className="text-xl font-bold text-[#f0f0f0] sm:text-2xl">Support Tickets</h1><p className="text-sm text-[#9ca3af]">Track complaints, payments, KYC and account support requests.</p></div><section className="overflow-hidden rounded-xl border border-[#2a2c30] bg-[#1b1c1e]"><table className="w-full text-left text-sm"><thead className="bg-[#242628] text-xs uppercase text-[#9ca3af]"><tr><th className="px-4 py-3">Ticket</th><th className="px-4 py-3">Subject</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{tickets.map(([id,subject,status])=><tr key={id} className="border-t border-[#2a2c30]"><td className="px-4 py-3 font-black text-[#ffdf19]">{id}</td><td className="px-4 py-3 text-[#f0f0f0]">{subject}</td><td className="px-4 py-3 text-[#9ca3af]">{status}</td></tr>)}</tbody></table></section></div>}
